appId: com.mitable.app
productName: Mitable
copyright: Copyright © 2025 Mitable
directories:
  buildResources: build
  output: dist

# macOS Configuration
mac:
  target:
    - target: dmg
      arch:
        - x64
        - arm64
  category: public.app-category.productivity
  # icon: build/icon.icns  # TODO: Add custom icon
  hardenedRuntime: true
  gatekeeperAssess: false
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

# DMG Configuration
dmg:
  title: "${productName} ${version}"
  # icon: build/icon.icns  # TODO: Add custom icon
  iconSize: 100
  contents:
    - x: 410
      y: 150
      type: link
      path: /Applications
    - x: 130
      y: 150
      type: file

# Windows Configuration
win:
  target:
    - target: nsis
      arch:
        - x64
  # icon: build/icon.ico  # TODO: Add custom icon
  # Disable code signing for dev builds (enable for production)
  sign: null
  certificateSubjectName: null

# File associations and metadata
files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{.env,.env.*,.editorconfig,.gitattributes,.gitignore,.yarnrc.yml,.travis.yml,appveyor.yml,circle.yml}"
  - "!{npm-debug.log,yarn.lock,.npmrc,yarn-error.log}"

# Build artifact configuration
artifactName: "${productName}-${version}-${arch}.${ext}"
