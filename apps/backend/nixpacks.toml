[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = [
  "npm ci || npm install",
  "npm ls @rollup/rollup-linux-x64-gnu 2>/dev/null || npm install @rollup/rollup-linux-x64-gnu --save-optional"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
