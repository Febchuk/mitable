{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ls @rollup/rollup-linux-x64-gnu 2>/dev/null || npm install @rollup/rollup-linux-x64-gnu --save-optional; npm rebuild sharp --platform=linux --arch=x64; npm run build --workspace=packages/shared && npm run build --workspace=apps/backend"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@mitable/backend"
  }
}
